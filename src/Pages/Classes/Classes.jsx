import React, { useEffect, useState } from 'react';

const Classes = () => {
  const [clssses, setClasses] = useState([]);

  useEffect(()=>{
    fetch('')
  },[])

  return (
    <div>
      
    </div>
  );
};

export default Classes;